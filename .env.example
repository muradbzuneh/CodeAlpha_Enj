# Vite Environment Variables
# 
# Configure the API base URL for the Express backend.
# The frontend switches between mock mode (default) and live mode based on this variable.

VITE_API_URL=http://localhost:4001
# Switch to 'true' to use the real backend instead of mock data
# VITE_USE_LIVE_API=true